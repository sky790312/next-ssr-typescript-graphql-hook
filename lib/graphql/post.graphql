query Posts {
  posts {
    id
    title
    recommendPosts {
      id
      title
    }
  }
}

# query PostDetail($postId: ID!) {
#   post(id: $postId) {
#     id
#     title
#     author {
#       name
#     }
#   }
# }
